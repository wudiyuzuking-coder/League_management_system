# 需求文档索引

课程要求与原始需求设计文档继续保留在项目根目录，避免移动后造成引用失效。本目录用于后续提交材料中的需求文档索引，不复制或改写正式需求基线。

当前最终提交与验收采用四角色模型：`USER`、`CLUB`、`EVENT_ADMIN`、`ADMIN`。职责及演示流程以项目根目录 `README.md` 为准。

项目历史版本曾实现独立CHECKER检票模块；当前四角色提交版本未将该模块纳入正式验收范围，相关代码和历史资料继续保留用于版本追溯。该范围调整不等于删除历史实现。
