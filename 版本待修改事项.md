# 版本待修改事项

记录当前版本在本地部署、运行和体验过程中发现的后续修改点。

## 环境与部署

- README 建议补充 Windows 下 MySQL 未加入 PATH 时的处理方式，例如使用 `C:\Program Files\MySQL\MySQL Server 26.7\bin\mysql.exe`。
- README 中 `mysql -e "source ..."` 在当前 MySQL 客户端版本下可能无法执行，建议补充使用输入重定向导入 SQL 的方案。
- 建议增加一份 `.env.example`，列出后端 `dev` profile 所需变量：`SPRING_PROFILES_ACTIVE`、`DB_URL`、`DB_USERNAME`、`DB_PASSWORD`、`JWT_SECRET`、`JWT_EXPIRATION_MINUTES`、`DEMO_PASSWORD_INIT_ENABLED`、`DEMO_PASSWORD`。
- 建议提供一键启动脚本，例如 `scripts/start-backend-dev.ps1` 和 `scripts/start-frontend.ps1`，减少手动配置出错。

## 数据库

- SQL 初始化需要严格保证 UTF-8 编码，否则中文数据和注释可能乱码，建议在 README 中明确 Windows 命令行使用 UTF-8。
- `test-data.sql` 建议增加执行前说明：首次运行可重建空库，已有数据环境需谨慎导入。
- 演示密码初始化完成后，应提醒关闭 `DEMO_PASSWORD_INIT_ENABLED`，避免后续误操作。

## 前端

- 注册页面公开注册固定创建 `USER` 角色，当前无法选择以 `USER`、`ADMIN`、`CLUB`、`CHECKER` 中的哪种身份注册；如课程演示需要，应评估是否增加身份选择入口，或更清楚地提示管理员/俱乐部/检票员账号只能由管理员后台创建。
- 管理员、俱乐部、检票员账号需要通过管理员后台创建，建议登录页或 README 增加说明。
- 检查前端源文件中的中文显示是否存在乱码，统一保存为 UTF-8。
- 前端启动依赖 Node.js 20.19+ 或 22.12+，建议 README 增加 Windows 安装后的 PATH 检查步骤：`node -v`、`npm -v`。
- 用户购票完成后，需要在订单详情、电子票详情或“我的订单/我的球票”中提供清晰的退票入口，避免用户支付成功后找不到申请退票的位置。
- 退票入口应根据订单/球票状态控制显示，例如仅对已支付且未使用、未退票、符合退票时间规则的订单或票展示申请退票按钮。
- 用户浏览球赛时，需要提供查看参赛球队信息的入口，例如在比赛列表、比赛详情页展示主队/客队名称并可点击进入球队详情。
- 球队详情建议展示俱乐部基础信息、球员名单、教练信息、近期战绩或积分排名，帮助用户在购票前了解比赛双方。

## 后端

- `local` profile 只能健康检查，`dev` profile 才包含完整业务接口。建议 README 更突出这一点，避免 `/api/auth/login` 在 local profile 下返回 404 被误认为接口缺失。
- 建议后端启动时打印当前 active profile，方便排查是否启动到了 `local` 或 `dev`。
- 建议增加启动失败时的常见错误说明，例如数据库连接失败、JWT_SECRET 未设置、端口 8080 被占用。

## 账号与权限

- 公开注册接口不接收 `roleCode`，后端固定赋予 `USER`，这是合理的安全设计。
- 管理员创建用户时可以选择 `USER`、`ADMIN`、`CLUB`、`CHECKER`。
- `CLUB` 和 `CHECKER` 账号必须绑定俱乐部，`USER` 和 `ADMIN` 不应绑定俱乐部。

## 已验证状态

- MySQL 数据库 `league_ticket` 已成功初始化。
- 后端 `dev` profile 已成功启动，健康检查 `/api/health` 返回 `UP`。
- 演示账号 `demo_admin / 123456` 登录接口验证成功。
- 前端 Vite 服务已成功启动，访问地址为 `http://localhost:5173`。


## 注册身份改造需求

- 目标：公开注册入口需要支持四种身份注册：`USER`、`ADMIN`、`CLUB`、`CHECKER`，不再只能固定注册为普通用户。
- 注册页需要增加身份选择，并根据所选身份动态展示不同表单字段、校验规则和提交说明。
- `USER` 注册需求：用户名、姓名、手机号、密码、确认密码，可直接注册为启用或按产品要求保持当前直接启用。
- `ADMIN` 注册需求：除基础账号信息外，应增加管理员邀请码、审核码或后台审批机制，避免任何人直接注册管理员账号。
- `CLUB` 注册需求：除基础账号信息外，需要填写或选择俱乐部信息，例如俱乐部名称、联系人、联系电话；账号最终应绑定 `clubId`，可考虑先提交申请再由管理员审核。
- `CHECKER` 注册需求：除基础账号信息外，需要选择/填写所属俱乐部，并提供检票员身份信息；账号最终应绑定 `clubId`，可考虑由俱乐部或管理员审核。
- 后端注册接口需要从只接收 `RegisterRequest` 改造成可接收 `roleCode` 及各身份专属字段，并在服务层按角色分别校验。
- 权限安全要求：即使前端允许选择身份，后端也必须限制 `ADMIN`、`CLUB`、`CHECKER` 的注册条件，不能只靠前端隐藏或提示来保证安全。
- 需要确认的产品规则：四类身份注册后是立即启用，还是部分身份进入待审核状态；如果需要审核，还需要新增申请状态和管理员审核入口。


