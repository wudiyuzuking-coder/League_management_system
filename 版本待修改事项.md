# 版本待修改事项

记录当前版本在本地部署、运行和体验过程中发现的后续修改点。已完成的问题已从本文件删除。

## 环境与部署

- README 建议补充 Windows 下 MySQL 未加入 PATH 时的处理方式，例如使用 `C:\Program Files\MySQL\MySQL Server 26.7\bin\mysql.exe`。
- README 中 `mysql -e "source ..."` 在当前 MySQL 客户端版本下可能无法执行，建议补充使用输入重定向导入 SQL 的方案。
- 建议增加一份 `.env.example`，列出后端 `dev` profile 所需变量：`SPRING_PROFILES_ACTIVE`、`DB_URL`、`DB_USERNAME`、`DB_PASSWORD`、`JWT_SECRET`、`JWT_EXPIRATION_MINUTES`、`DEMO_PASSWORD_INIT_ENABLED`、`DEMO_PASSWORD`。

## 数据库

- `test-data.sql` 建议增加执行前说明：首次运行可重建空库，已有数据环境需谨慎导入。

## 前端

- 检查前端源文件中的中文显示是否存在乱码，统一保存为 UTF-8。
- 前端启动依赖 Node.js 20.19+ 或 22.12+，建议 README 增加 Windows 安装后的 PATH 检查步骤：`node -v`、`npm -v`。
- 用户浏览球赛时，需要提供查看参赛球队信息的入口，例如在比赛列表、比赛详情页展示主队/客队名称并可点击进入球队详情。
- 球队详情建议展示俱乐部基础信息、球员名单、教练信息、近期战绩或积分排名，帮助用户在购票前了解比赛双方。

## 账号与登录

- 注册后允许用户修改用户名和头像。
- 登录方式调整为手机号登录；手机号必须全系统唯一，用户名允许重复。
- 用户名改为展示昵称性质，不再作为登录唯一凭证。
- 账号资料中建议区分手机号、用户名、真实姓名/工号/俱乐部名称等字段，避免后续角色资料混用。

## 演示时间系统

- 系统需要建立独立的“当前时间”配置，所有涉及时间判断的业务均使用系统当前时间，而不是直接使用电脑真实时间。
- 所有角色均可以调整系统当前时间，调整后对全系统所有角色立即生效，便于课程演示赛季报名、开赛、购票、退票和赛后录入比分等流程。
- 时间调整建议记录操作日志，至少记录调整人、调整前时间、调整后时间和调整时间，便于演示时排查状态变化。
- 所有倒计时、比赛剩余天数、报名截止、售票开始/结束、退票截止和比分录入限制均需要接入统一系统时间。

## 用户端功能增强

- 用户端需要完善赛季、赛程、俱乐部、比赛地点、赛场座位和购票价格之间的展示链路。
- 俱乐部详情页需要展示该俱乐部已报名或已安排的比赛，并突出最近比赛还有几天开始。
- 比赛详情页需要展示比赛地点、场馆看台、座位区域、票区价格、余票和可购买信息。
- 用户从赛程或比赛详情进入购票时，应能清楚看到当前比赛对应的场馆座位结构和各票区价格。

## 俱乐部负责人报名流程

- 俱乐部负责人注册并通过审核后，可以查看当前可报名赛季。
- 俱乐部仅可报名时间不冲突的赛季；如果赛季时间与已报名赛季冲突，需要提示不可报名原因。
- 俱乐部报名前需要填写主场场馆信息，场馆固定分为东、西、南、北四个看台，每个看台分为内场 VIP 和外场普通两个区域。
- 主场场馆信息需要包含地址、座位数和定价信息；座位数至少包括内场 VIP 排数、外场普通排数、长边每排座位数、宽边每排座位数。
- 主场定价需要区分内场 VIP 定价和外场普通定价。
- 报名时需要填写球员信息，包括姓名、位置、号码、出生年和首发/替补状态；位置限定为前锋、中场、后卫、守门员。
- 系统根据统一当前时间和出生年计算球员年龄并展示，不直接展示生日。
- 报名时需要填写教练信息，教练年龄展示规则与球员一致。
- 报名校验要求至少提交 11 名球员和 1 名教练。
- 俱乐部负责人需要可以查看已报名赛季和已报名比赛，并显示最近比赛距离系统当前时间还有几天。

## 赛事管理员功能增强

- 赛事管理员注册成功后，账号仍需按安全规则等待启用或审核，不应直接获得赛事管理权限。
- 赛事管理员可以注册新赛季，并设置球队数量上限，单赛季球队数量上限不得超过 20 支。
- 注册赛季时需要提供比赛开始时间，且比赛开始时间距离赛季注册时间至少 1 个月。
- 注册赛季时需要设置报名截止时间，报名截止时间不得晚于比赛开始时间前 1 周。
- 当报名截止时间到达，或报名球队数量达到上限后，系统自动安排赛程。
- 赛程生成规则采用主客场双循环，同一支球队两场比赛之间至少间隔 6 天。
- 系统生成赛程后，由赛事管理员进行审核确认；审核操作简化为点击确认即可。
- 赛后比分只能在比赛当天及以后录入，未到比赛日期时不得录入比分。
- 需要为赛事管理员增加赛后比分录入提醒，提示其及时维护比赛结果。
- 票务管理规则后续需要继续细化，包括票区开售、价格调整、售票时间和库存生成等。

## 系统管理员功能增强

- 系统默认保留一个初始系统管理员账号，用于首次进入系统和创建后续管理账号。
- 系统管理员负责注册新的赛事管理员和系统管理员账号。
- 创建赛事管理员和系统管理员时需要填写姓名和工号。
- 赛事管理员工号格式为 `EA` 加 4 位数字，例如 `EA0001`。
- 系统管理员工号格式为 `SA` 加 4 位数字，例如 `SA0001`。
- 系统需要校验管理人员工号格式和唯一性。

## 后端

- 建议增加启动失败时的常见错误说明，例如数据库连接失败、JWT_SECRET 未设置、端口 8080 被占用。
