# 设计文档索引

数据库阶段0正式字段字典位于 `database/阶段0字段字典.md`。各阶段的最终业务口径、接口和安全迁移说明集中保留在项目根目录 `README.md`。

当前交付设计固定为 `USER`、`CLUB`、`EVENT_ADMIN`、`ADMIN` 四类正式角色。独立CHECKER检票模块属于历史实现，代码和设计记录仍保留，但不进入本次最终演示、默认测试基线或验收结论。
