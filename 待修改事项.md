# 待修改事项

## 运行与兼容问题

- 已确认本机 MySQL 服务 `MySQL267` 正在运行，MySQL 客户端路径为 `C:\Program Files\MySQL\MySQL Server 26.7\bin\mysql.exe`。
- 已确认数据库 `league_ticket` 存在，当前有 25 张表。
- 已确认 5173 和 8080 端口当前未被监听，前后端尚未启动。
- 旧版浏览器缓存可能保留旧角色值，导致 Vue Router 在登录页和角色首页之间无限重定向。后续需要在前端加入 `USER`、`CLUB`、`EVENT_ADMIN`、`ADMIN` 白名单兼容处理，不支持的缓存角色应清理登录态并回到登录页。
- `scripts/env.ps1` 原先包含作者本机路径和 3315 端口内置 MySQL 配置。后续需要调整为支持本机配置或环境变量，避免直接依赖作者电脑路径。
- README 原先使用 `mysql -e "source ..."` 导入数据库；部分 Windows MySQL 客户端不可用。后续建议补充 `cmd /c "chcp 65001 > nul && mysql ... < database/*.sql"` 的替代导入方式。

## 后续功能需求

- 注册界面不再开放赛事管理员和系统管理员注册，只保留普通用户和俱乐部负责人注册入口。
- 不同身份注册要求不同：普通用户填写姓名；俱乐部负责人填写俱乐部信息，提交后等待后台审核。
- 登录界面需要增加身份下拉选择框，由用户选择以普通用户、俱乐部负责人、赛事管理员或系统管理员身份登录。
- 赛事管理员和系统管理员登录时除手机号、密码外，还需要填写对应工号，并由后端校验手机号、密码、身份和工号是否匹配。
- 用户买完票后需要有明显的退票入口，可从订单列表、订单详情或电子票入口进入退票流程。
- 用户需要有查看比赛双方球队信息的入口，建议从比赛详情页的主队/客队区域进入俱乐部详情。
- 每场比赛应在开赛前 7 天的晚上 20:00 自动开启票务购买服务，未到开启时间前用户只能查看比赛和票务信息，不能下单购票。
- 俱乐部注册审核通过后，应自动创建或关联俱乐部资料，并直接出现在俱乐部管理中。
- 每个俱乐部都应该有一个俱乐部负责人，俱乐部管理列表和详情页需要展示负责人姓名、手机号和账号状态。
- 登录失败提示需要更明确：用户名不在数据库中时提示“用户名不存在”，用户名存在但密码错误时提示“密码错误”。
