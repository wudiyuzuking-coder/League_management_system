# 更新日志

## 2026-09-02（阶段19）

### 最终审计与版本冻结
- 在全新隔离MySQL 8实例完成30表冷启动、seed/test-data二次幂等和phase16b至17c迁移链实跑；迁移后列、索引、约束与当前schema一致。
- 完成107项测试、未跳过测试的clean package、可执行JAR真实启动、前端生产构建、四角色HTTP权限矩阵和浏览器主流程复核。
- 完成USER购票/退款、CLUB审核/报名、4队6轮12场双循环及确认、SystemTime回拨、比分/提醒、统计、头像和同昵称身份隔离E2E。
- 将旧待办逐项分类为DONE、DEFERRED、CONFLICT_RESOLVED或CONFLICT_REMAINS；明确冻结范围和已知延期项，本阶段不新增业务功能。

## 2026-09-01（阶段18A）

### USER俱乐部详情与比赛购票信息链
- 比赛列表与详情中的主客队可进入安全的USER俱乐部公开详情，展示基础资料、有效阵容、教练、积分及近期赛程。
- 下一场剩余天数和球员年龄统一使用`SystemTimeService`，公开响应排除账号、工号、出生日期、报名与锁座等私有/技术字段。
- 比赛详情补充场馆地址、容量、座位结构、票价、余票、最大连坐和后端判定的当前可购状态。
- 数据库仍为30张表，未修改连坐、订单、支付、退票状态机；新增7项专项测试，正式测试基线为107项。

## 2026-09-01（阶段17C）

### 用户资料与头像
- `sys_user`新增可空的`avatar_url`，四类正式角色统一使用账号资料页并保持工号、clubId等角色字段语义。
- 新增登录用户头像上传和移除、公开静态读取；支持JPEG/PNG、最大2MB，并校验扩展名、Content-Type和真实图片内容。
- 本地头像使用UUID文件名和可配置`APP_UPLOAD_DIR`，运行时文件不进入Git；补充阶段17C迁移脚本和6项专项测试。
- 顶部栏与资料页共享当前用户状态，昵称和头像保存后即时刷新。

## 2026-08-31（阶段17B）

### 手机号登录与昵称化
- 登录请求统一为`phone + password`，不再接受username兼容登录；手机号UNIQUE约束保留。
- `username`改为允许重复、允许修改的昵称，移除唯一索引并保留普通查询索引。
- JWT subject改为稳定`userId`，认证过滤器按userId重新读取账号、角色、权限和clubId；旧Token需要重新登录。
- 前端登录页、资料页、ADMIN用户表和四角色顶部显示同步新语义；新增阶段17B迁移脚本与手机号身份专项测试。

## 2026-08-31

### 阶段17A账号资料规范化
- 为`sys_user`新增可空且全局唯一的`employee_no`，EVENT_ADMIN使用`EA####`，ADMIN使用`SA####`。
- 管理账号真实姓名与工号分开存储，公开申请、后台创建、编辑和启用统一使用Service校验。
- USER/CLUB禁止设置管理人员工号；历史管理账号允许字段暂时为空，但启用前必须补齐。
- 注册页和ADMIN用户管理页分别展示姓名与工号，演示管理账号回填`EA0001/SA0001`。

## 2026-08-30（阶段16A—16D）

### 阶段16A：统一系统时间
- 增加基于全局偏移的`SystemTimeService`，四类正式角色可查看、调整和恢复演示时间，并记录操作日志。
- 报名、售票、订单、支付、退票、比赛限制与倒计时逐步统一到系统时间；匿名修改被拒绝。

### 阶段16B：CLUB赛季报名
- 为赛季增加报名时间窗和1至20的容量；新增报名主表、球员快照和教练快照，保持`club_season_record`仅表示战绩。
- 实现日期闭区间冲突、默认主场、人员归属、至少11球员和1教练、重复/容量并发及EVENT_ADMIN只读查看。

### 阶段16C：自动双循环排赛与确认
- 使用确定性Circle Method生成主客场双循环，相邻轮至少6天；支持满额AFTER_COMMIT、截止Scheduler和人工补偿触发。
- 新增每赛季唯一赛程批次和比赛关联，EVENT_ADMIN确认后CLUB只读取自己的已确认赛程；确认不自动发布比赛或生成库存。

### 阶段16D：比分日期限制与赛果提醒
- 比赛日期之前禁止录分；当天及以后仍遵守原比赛状态机，完赛后重算积分榜。
- EVENT_ADMIN赛果提醒由现有比赛与统一系统日期动态计算，完赛后自动消失，不新增提醒表或推送系统。

## 2026-08-28

### 角色体系调整
- 将原“俱乐部”角色优化为“俱乐部负责人”。
- 将“检票员”移出当前正式角色和演示账号范围；相关历史代码与测试另行保留用于版本追溯。
- 新增“赛事管理员”角色，负责比赛管理、赛季轮次、比赛结果录入、票务配置、退票审核和赛事统计。
- 将原“管理员”明确为“系统管理员”，主要负责用户管理和俱乐部注册审核。

### 权限与菜单优化
- 系统管理员仅保留用户管理、俱乐部注册审核等系统管理功能。
- 赛事管理员拥有比赛、赛季、场馆、票务、退票和统计相关功能权限。
- 修复赛事管理员进入“比赛管理”时提示“没有权限执行此操作”的问题。
- 根据不同角色调整后台首页跳转和左侧菜单显示。

### 注册流程优化
- 注册页基础信息调整为：用户名、手机号、密码、确认密码。
- 新增“注册身份”下拉选择框，默认未选择。
- 未选择注册身份时，不显示身份专属信息和注册按钮。
- 选择不同身份后，显示对应必填信息：
  - 用户：姓名
  - 俱乐部负责人：俱乐部名字
  - 赛事管理员：工号
  - 系统管理员：工号
- 为身份专属必填项增加红色星号提示。
- 后端同步要求注册时必须明确选择身份。

### 演示账号调整
- 系统管理员账号：`demo_admin`，密码：`123456`
- 赛事管理员账号：`demo_event_admin`，密码：`123456`
- 俱乐部负责人账号：`demo_club`，密码：`123456`
- 普通用户账号：`demo_user`，密码：`123456`

### 验证情况
- 前端构建通过。
- 后端编译通过。
- 注册页交互验证通过。
- 四种角色注册接口验证通过。
- 页面控制台未发现错误。
